export default function getCategoriesTecno(categories) {
  return [categories[13], categories[23],
    categories[16], categories[10], categories[20]];
}

export function getCategoriesOthers(categories) {
  return [categories[0], categories[6],
    categories[7], categories[8], categories[9], categories[11],
    categories[12], categories[15], categories[17], categories[21], categories[25],
  ];
}
